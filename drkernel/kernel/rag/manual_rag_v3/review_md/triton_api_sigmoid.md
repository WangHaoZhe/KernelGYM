# triton.language.sigmoid — Triton  documentation

- `source_id`: `triton_api_sigmoid`
- `url`: https://triton-lang.org/main/python-api/generated/triton.language.sigmoid.html
- `category`: `triton_api`
- `num_chunks`: `1`
- `tags`: `triton, sigmoid, activation`

## Chunk 0

- `chunk_id`: `triton_api_sigmoid::chunk::0`
- `token_estimate`: `44`
- `word_count`: `44`

~~~text
# triton.language.sigmoid

triton.language. sigmoid ( x )

Computes the element-wise sigmoid of x . Parameters : x ( Block ) – the input values This function can also be called as a member function on tensor , as x.sigmoid(...) instead of sigmoid(x, ...) .
~~~
