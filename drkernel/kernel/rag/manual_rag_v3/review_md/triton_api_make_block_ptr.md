# triton.language.make_block_ptr — Triton  documentation

- `source_id`: `triton_api_make_block_ptr`
- `url`: https://triton-lang.org/main/python-api/generated/triton.language.make_block_ptr.html
- `category`: `triton_api`
- `num_chunks`: `1`
- `tags`: `triton, block_ptr, memory`

## Chunk 0

- `chunk_id`: `triton_api_make_block_ptr::chunk::0`
- `token_estimate`: `83`
- `word_count`: `83`

~~~text
# triton.language.make_block_ptr

triton.language. make_block_ptr ( base : tensor , shape , strides , offsets , block_shape , order , _semantic = None )

Returns a pointer to a block in a parent tensor Parameters : base – The base pointer to the parent tensor shape – The shape of the parent tensor strides – The strides of the parent tensor offsets – The offsets to the block block_shape – The shape of the block order – The order of the original data format
~~~
